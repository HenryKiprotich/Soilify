<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoilifyMobileApp"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SoilifyMobileApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SignUpActivity" />
        <activity android:name=".ui.DashboardActivity" />
        <activity android:name=".ui.RecordFertiliserActivity" />
        <activity android:name=".ui.AnalyticsActivity" />
        <activity android:name=".ui.AIActivity" />
        <activity android:name=".ui.AlertsActivity" />
        <activity android:name=".ui.FieldsActivity" />
        <activity android:name=".ui.WeatherDataActivity" />
        <activity android:name=".ui.NewFieldActivity" />
        <activity android:name=".ui.EditFieldActivity" />
        <activity android:name=".ui.NewWeatherDataActivity" />
        <activity android:name=".ui.EditWeatherDataActivity" />
    </application>

</manifest>